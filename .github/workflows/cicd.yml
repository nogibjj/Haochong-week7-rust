name: CI

on:
  push:
    branches:
      - main  # Change to your main branch name

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Rust
        uses: actions/checkout@v2
        with:
          repository: nogibjj/Haochong-week7-rust-data-engineering
          path: caesar-cipher-cli  # Specify the subdirectory

      - name: Change to caesar-cipher-cli directory
        run: cd caesar-cipher-cli

      - name: Build and Test
        run: |
          cargo build --verbose
          cargo test --verbose

      - name: Deploy
        run: make deploy
